// Array / String / Object / Number methods

// type checking / conversion
/**
 * typeof(var) === 'number' checks if var is a number
 * conversion converts one data type to another for example
 * a '4' string into an integer 4. Integer.parseInt('4') -> 4
 * */ 

// JS operators (== vs ===) etc.
/** 
 * '==' is for comparing value but not data type, 
 * '===' is for strictly checking both value and type.
 * 4 == '4' -> true 
 * 4 === '4' -> false
 * */ 

// Asynchronous Code

// AJAX

// XHR / Fetch

// Callbacks / Promises / Async Await

// Axios
/**
 * Axios is a promise based HTTP client for the browser and Node.js. 
 * Axios makes it easy to send asynchronous HTTP requests to REST endpoints 
 * and perform CRUD operations. It can be used in plain JavaScript or with a 
 * library such as Vue or React.
 */
