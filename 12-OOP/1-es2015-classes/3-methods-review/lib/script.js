// const add = (a, b) => a + b;
// const mult = (a, b) => a * b;
// const square = (a) => a ** 2;
// const power = (a, b) => a ** b;

// const myMath = {};
// myMath.add = add;
// myMath.mult = mult;

// const myMath = {add, mult, square, power};

const myMath = {
  add(a, b) {
    return a + b;
  },
  mult(a, b) {
    return a * b;
  }
};

// console.log(myMath.add(3,4));
